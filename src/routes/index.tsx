// Stub index route — required by the TanStack Router generator running in
// the Lovable preview sandbox. The real home page is rendered via
// react-router-dom in src/App.tsx, so this file is intentionally empty.
export {};